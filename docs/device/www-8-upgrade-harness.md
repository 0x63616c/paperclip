# Paperclip failure harness

Generated by `paperclip-failure-harness`. Every row below was produced by causing the
failure on this machine and then reading unit states, cgroup membership and the pid
table — not by asserting that a function was called.

```
machine   Debian GNU/Linux 12 (bookworm)
arch      aarch64
kernel    6.1.0-53-cloud-arm64
systemd   systemd 252 (252.39-1~deb12u2)
```

**This is a Linux VM, not the tablet.** It proves the supervision path is enforced by
a real systemd on aarch64. It is not device qualification, and the isolation report it
prints for this machine is not the one that applies to the Paper Pro — see the
`facilities` case, which asserts the two are different.

28/28 cases passed.

| Case | Requirement | Result | Took |
|---|---|---|---|
| `facilities` | §11 — the isolation report describes THIS machine, not a wish list | **pass** | 0.0s |
| `directives-applied` | §11 — what the unit says is what systemd is enforcing | **pass** | 1.2s |
| `app-crash-panic` | App/Home crash — terminate remaining processes, release display, restore stock | **pass** | 3.9s |
| `app-crash-abort` | App/Home crash — abort() is a signal death, not an exit code | **pass** | 3.7s |
| `app-hang` | App hang — deadline, graceful termination, bounded force termination | **pass** | 4.3s |
| `heartbeat-is-not-progress` | App hang — a heartbeat on an unrelated thread does not prove the UI works | **pass** | 4.4s |
| `ignored-termination` | App hang — a session that refuses SIGTERM is still bounded | **pass** | 6.7s |
| `lingering-children` | Child survives parent — contain and terminate the complete tree | **pass** | 3.8s |
| `display-process-crash` | Display process crash — terminate the dependent session, restore stock | **pass** | 1.7s |
| `host-crash` | Host crash — an independent systemd path cleans up and restores stock | **pass** | 1.4s |
| `ssh-disconnect` | SSH disconnect — the session does not depend on the terminal that started it | **pass** | 2.2s |
| `repeated-failures` | Repeated failures — stop retrying, no restart loops, diagnostics left behind | **pass** | 15.8s |
| `stock-fails-to-start` | Xochitl fails to start — explicit failed state, preserved logs, no false claim | **pass** | 102.6s |
| `start-budget-refusal` | Never let stock fail — refuse to take the display near StartLimitBurst | **pass** | 6.0s |
| `reboot` | Reboot — stock startup stays the default; no automatic takeover | **pass** | 0.2s |
| `memory-pressure` | Extra hazard — memory exhaustion is contained and attributable | **pass** | 3.7s |
| `disk-full` | Extra hazard — a full writable grant does not take the device with it | **pass** | 3.6s |
| `write-outside-grants` | Extra hazard — a write outside the granted paths is refused | **pass** | 3.8s |
| `upgrade-healthy` | §13 — a healthy release is committed and the old one becomes the fallback | **pass** | 0.4s |
| `upgrade-with-active-session` | §13 — a healthy upgrade stands a running session down before the swap | **pass** | 2.9s |
| `upgrade-panics` | §13 — a release that panics on start is rolled back | **pass** | 0.5s |
| `upgrade-never-ready` | §13 — a release that starts but never reaches `ready` is rolled back | **pass** | 35.5s |
| `upgrade-middle-rung` | §13 — a release that stalls mid-ladder is rolled back, naming the rung | **pass** | 35.3s |
| `upgrade-power-loss` | §13 — killed between ACTIVATE and COMMIT, reconcile reverts rather than resumes | **pass** | 0.2s |
| `upgrade-reboot` | §13 — a reboot mid-update leaves stock startup available and a reconcilable journal | **pass** | 0.0s |
| `setup-is-idempotent` | §14 — setup inspects real prerequisites, and running it twice changes nothing | **pass** | 0.0s |
| `upgrade-cannot-replace-the-bootstrap` | §13 — an ordinary upgrade replaces nothing outside releases/ | **pass** | 0.4s |
| `install-power-loss` | §12 — an app install SIGKILLed mid-transaction leaves the old release usable | **pass** | 0.8s |

## What each case observed

### `facilities` — pass

§11 — the isolation report describes THIS machine, not a wish list

- probed: probed on aarch64 6.1.0-53-cloud-arm64
- cgroup unified / memory Delegated
- seccomp true / bpf Some(false) / lsm lockdown,capability,landlock,yama,apparmor,tomoyo,bpf
- tree termination: single write to cgroup.kill
- unclaimable here: Writable storage, CPU bound, Mandatory access control, Network reach
- unclaimable on the Paper Pro: Writable storage, Memory bound, CPU bound, Syscall filtering, Mandatory access control, Network reach

### `directives-applied` — pass

§11 — what the unit says is what systemd is enforcing

- User=paperclip
- ProtectSystem=strict
- NoNewPrivileges=yes
- DevicePolicy=closed
- LimitAS=201326592
- TasksMax=32
- MemoryMax=201326592
- the same generator writes no MemoryMax= for the Paper Pro

### `app-crash-panic` — pass

App/Home crash — terminate remaining processes, release display, restore stock

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.panic.service cgroup empty
- paperclip-session.target inactive
- unit Result=exit-code
- diagnostics bundle written to /run/paperclip-harness/diagnostics/last-failure.txt

### `app-crash-abort` — pass

App/Home crash — abort() is a signal death, not an exit code

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.abort.service cgroup empty
- paperclip-session.target inactive
- Result=signal ExecMainCode=2

### `app-hang` — pass

App hang — deadline, graceful termination, bounded force termination

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.hang.service cgroup empty
- paperclip-session.target inactive
- detected and recovered in 3.1s (stall budget 2.5s)
- every process of the hung session is gone

### `heartbeat-is-not-progress` — pass

App hang — a heartbeat on an unrelated thread does not prove the UI works

- 1 process(es) alive and responsive when the hang began
- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.heartbeat.service cgroup empty
- paperclip-session.target inactive
- a live, signal-responsive process was still classified as hung

### `ignored-termination` — pass

App hang — a session that refuses SIGTERM is still bounded

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.stubborn.service cgroup empty
- paperclip-session.target inactive
- 1 process(es) that ignored SIGTERM were force-terminated inside the budget

### `lingering-children` — pass

Child survives parent — contain and terminate the complete tree

- 4 orphaned child process(es) observed
- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.leavers.service cgroup empty
- paperclip-session.target inactive
- every orphan was contained and terminated

### `display-process-crash` — pass

Display process crash — terminate the dependent session, restore stock

- display owner was pid 6057; registry cleared
- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.display.service cgroup empty
- paperclip-session.target inactive
- the dependent session was terminated rather than left holding nothing

### `host-crash` — pass

Host crash — an independent systemd path cleans up and restores stock

- supervisor pid 6091 killed with SIGKILL
- paperclip-restore-stock.service ran from systemd's OnFailure= with no supervisor alive
- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.orphaned.service cgroup empty
- paperclip-session.target inactive

### `ssh-disconnect` — pass

SSH disconnect — the session does not depend on the terminal that started it

- stand-in login session 6135 killed
- session and supervisor still running (1 process(es))
- session cgroup is /paperclip.slice/paperclip-app@dev.calum.remote.service — systemd's, not a login scope
- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.remote.service cgroup empty
- paperclip-session.target inactive

### `repeated-failures` — pass

Repeated failures — stop retrying, no restart loops, diagnostics left behind

- crash 1 recovered to stock
- crash 2 recovered to stock
- crash 3 recovered to stock
- supervisor stopped relaunching after its failure budget
- a further request left the device at stock (NRestarts 0 -> 0)
- diagnosis: the foreground session failed 3 times within 120s; not retrying

### `stock-fails-to-start` — pass

Xochitl fails to start — explicit failed state, preserved logs, no false claim

- supervisor state is `failed`
- 26475 bytes of preserved logs at /run/paperclip-harness/diagnostics/last-failure.txt
- no claim of a successful recovery anywhere in the status
- `paperctl stock` failed explicitly and independently of the supervisor
- stock start attempts recorded: 0

### `start-budget-refusal` — pass

Never let stock fail — refuse to take the display near StartLimitBurst

- three recent starts recorded — the whole device-level allowance
- paperclip-app@dev.calum.unlucky.service never started
- paperclip-harness-stock.service never stopped
- supervisor back at stock, having refused rather than begun

### `reboot` — pass

Reboot — stock startup stays the default; no automatic takeover

- no paperclip unit outside /run/systemd/system
- no paperclip unit is enabled
- /run is a tmpfs; the generated units do not survive a boot
- after clearing /run, systemd knows of no paperclip units at all

### `memory-pressure` — pass

Extra hazard — memory exhaustion is contained and attributable

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.hungry.service cgroup empty
- paperclip-session.target inactive
- session ended as Result=signal ExecMainStatus=6
- the supervisor survived the session's memory exhaustion
- bounded by MemoryMax= and LimitAS= here

### `disk-full` — pass

Extra hazard — a full writable grant does not take the device with it

- paperclip-harness-stock.service active again
- paperclip-app@dev.calum.filler.service cgroup empty
- paperclip-session.target inactive
- the supervisor and its state directory were unaffected
- supervisor could still write its status (108 bytes)

### `write-outside-grants` — pass

Extra hazard — a write outside the granted paths is refused

- root filesystem: /etc/paperclip-should-not-exist refused (exit 0)
- paperclip's own tree: /opt/paperclip-harness/bin/tampered refused (exit 0)
- device identity: /data/paperclip-should-not-exist refused (exit 0)

### `upgrade-healthy` — pass

§13 — a healthy release is committed and the old one becomes the fallback

- upgrade reported: Paperclip 0.1.0 -> 0.2.0: ready in 0.3s
- current 0.2.0, previous 0.1.0, journal at commit

### `upgrade-with-active-session` — pass

§13 — a healthy upgrade stands a running session down before the swap

- brought up paperclip-app@dev.calum.upgrade-session.service before upgrading
- the session was stood down before the binary underneath it moved
- upgrade reported: Paperclip 0.1.0 -> 0.2.0: ready in 0.6s
- current 0.2.0; the new supervisor is running and no session process survived the swap

### `upgrade-panics` — pass

§13 — a release that panics on start is rolled back

- a candidate whose supervisor panicked was refused and 0.1.0 came back
- report: 0.2.0 was refused: reached `control`, never reached `protocol`; the supervisor exited (scripted stall below `protocol`) | 0.1.0 is back: ready in 0.0s

### `upgrade-never-ready` — pass

§13 — a release that starts but never reaches `ready` is rolled back

- a release that went `active` without reaching `ready` was refused
- report: 0.2.0 was refused: reached `home`, never reached `ready` (scripted stall below `ready`) | 0.1.0 is back: ready in 0.0s

### `upgrade-middle-rung` — pass

§13 — a release that stalls mid-ladder is rolled back, naming the rung

- report: 0.2.0 was refused: reached `protocol`, never reached `device-adapter` (scripted stall below `device-adapter`) | 0.1.0 is back: ready in 0.0s
- stock is active and the wakelock is released

### `upgrade-power-loss` — pass

§13 — killed between ACTIVATE and COMMIT, reconcile reverts rather than resumes

- killed mid-transaction with SIGKILL; the journal survived
- reconcile: 0.2.0 never committed; 0.1.0 is selected again
- `current` is back at 0.1.0

### `upgrade-reboot` — pass

§13 — a reboot mid-update leaves stock startup available and a reconcilable journal

- the update journal is at /opt/paperclip-harness/state/update.json, outside /run
- the supervisor unit is runtime-only, resolves through `current`, and is never enabled
- reconcile on a clean tree: nothing was interrupted

### `setup-is-idempotent` — pass

§14 — setup inspects real prerequisites, and running it twice changes nothing

- setup reported every stage and found no prerequisite missing
- run twice; `current` stayed at 0.1.0

### `upgrade-cannot-replace-the-bootstrap` — pass

§13 — an ordinary upgrade replaces nothing outside releases/

- the bootstrap and the trusted keys are byte-identical after a platform upgrade
- nothing was written outside the release tree

### `install-power-loss` — pass

§12 — an app install SIGKILLed mid-transaction leaves the old release usable

- killed mid-transaction with SIGKILL while installing 0.2.0
- 0.1.0 stayed selected and launchable throughout
- recover: cleaned    1 staging directories | cleared    1 lock(s) left by a killed process
- staging and the journal were both swept clean
- found (not fixed here): 1 `.paperclip-tmp.*` file(s) left in state/journal — a kill between atomic_write's temp file and its rename is never swept by recover; harmless to selection, a permanent small leak

